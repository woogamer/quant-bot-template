services:
  bot:
    build: .
    container_name: quant-bot
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./my_strategy.py:/app/my_strategy.py:ro
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Seoul
